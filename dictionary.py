def build_histogram():
    fhand=open("mbox-short.txt")
    list=[]
    histogram=dict()
    for line in fhand:
        if line.find("@gmail.com"):
            list.append(line)
        if line.find()
    for word in list:
        histogram[word] = histogram.get(word,0) + 1

def 

